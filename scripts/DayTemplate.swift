import Algorithms
import Foundation
import Utilities

public struct {DAY_NAME}: Day {
    public func part1Solution(for input: Input) -> {RETURN_TYPE} {
        return {RETURN_VALUE}
    }
    
    public func part2Solution(for input: Input) -> {RETURN_TYPE} {
        return {RETURN_VALUE}
    }
}
